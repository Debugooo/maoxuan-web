# 《实践论》精读实操指南（mx-v1-017）内容更新 Spec

**目标**

- 将用户提供的《实践论》精读指南内容落盘到现有条目 `mx-v1-017`，保持站点统一的 `## 1–## 10` 模板结构与既有校验规则。
- 将“实操转化：重构你的认知源代码”迁移到训练与复盘页面（题库），指南页不出现该实操段落。
- 原文页面格式参照第一篇：不显示来源导航行；标题用 `###`；日期行使用中文年月格式（与既有 v1 原文一致的样式）。

---

## 范围

**包含**

- 修改指南：`content/maoxuan-guides/v1/mx-v1-017.md`
- 修改原文：`content/maoxuan-original/v1/mx-v1-017.md`
- 新增训练题：`src/data/questions.ts`
- 校验与预览：`npm run validate:study && npm run lint && npm run build`；抽查 `/study/mx-v1-017`、`/original/mx-v1-017`、`/training`

**不包含**

- 不新增“本周行动清单”
- 不变更站点渲染逻辑、页面组件或样式
- 不新增新的 `mx-v1-xxx` 条目（复用现有 `mx-v1-017`）

---

## 内容要求（指南页）

**文件**

- `content/maoxuan-guides/v1/mx-v1-017.md`

**Frontmatter**

- `id: mx-v1-017`（保持不变）
- `title: 实践论`（保持不变）
- `volume: v1`（保持不变）
- `prev_learning: mx-v1-016`
- `next_learning: mx-v1-018`
- `cos.stage/module` 保持与索引一致（现为 `stage: 1`, `module: kernel`）

**结构与排版约束**

- 必须使用统一模板标题：
  - `## 1 历史坐标`
  - `## 2 认知操作系统位置`
  - `## 3 回答的核心问题`
  - `## 4 阅读定位（正确读法与学法）`
  - `## 5 金句摘录`
  - `## 6 核心思想`
  - `## 7 关键概念（最小定义）`
  - `## 8 全文结构速览（认知地图）`
  - `## 9 金句摘录`
  - `## 10 与其他文章的联动（互文与学习顺序）`
- “逐段精读指引”必须放在 `## 9` 下（形如 `### 逐段精读指引`），并包含用户提供的 3.1–3.5 分段内容（不精简）。
- `## 9` 的高亮索引必须以 `- H01：...` ～ `- H12：...` 的形式出现，供校验脚本与原文高亮摘录对齐。
- 不出现用户提供的“第四部分：实操转化”内容（将迁移到训练题）。
- 不新增“七、本周行动清单”。
- 不改变既有站点对 `## 9` 与 H01–H12 的依赖关系（保持与 `validate:study` 兼容）。

---

## 内容要求（原文页）

**文件**

- `content/maoxuan-original/v1/mx-v1-017.md`

**格式约束（参照 v1-001）**

- 正文开头必须为：
  - `### 实践论`
  - 空行
  - `（一九三七年七月）`
- 不在正文顶部出现“中文马克思主义文库 -> 毛泽东”或类似来源导航行（若抓取源中包含则删除）。
- 保留正文现有内容（不做删节），但允许为满足开头格式做最小调整（例如将原文现有标题“论认识和实践的关系——知和行的关系”作为正文中的普通小标题或保持其现状）。
- 文末必须存在 `### 高亮摘录` 区块，包含 `==〔H01〕...==` ～ `==〔H12〕...==`，并与指南 `## 9` 的 H01–H12 文案逐字一致。

---

## 内容要求（训练与复盘）

**文件**

- `src/data/questions.ts`

**新增题目**

- 新增一题（建议 id：`shijian-4`）
- `chapterSlug` 使用现有 `shijianlun`（不新增 chapterSlug）
- `title` 与 `prompt` 使用用户提供的“第四部分：实操转化：重构你的认知源代码”（不精简、不拆分为多题）
- 标签 `tags` 需与现有 `shijian-*` 题目风格一致

---

## 验收标准

- `/study/mx-v1-017`：
  - 结构为 `## 1–## 10`
  - `## 9` 下存在 H01–H12 列表与“逐段精读指引”
  - 页面中不包含“实操转化”段落与行动清单
- `/original/mx-v1-017`：
  - 正文顶部显示 `### 实践论` 与 `（一九三七年七月）`
  - 不显示“中文马克思主义文库 -> 毛泽东”
  - 文末包含“高亮摘录”
- `/training`：
  - 题库中出现新增题“重构你的认知源代码”（或等价标题）
- 构建校验：
  - `npm run validate:study && npm run lint && npm run build` 均通过

